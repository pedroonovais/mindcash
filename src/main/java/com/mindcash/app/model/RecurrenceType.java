package com.mindcash.app.model;

public enum RecurrenceType {
    /** Recorrência mensal sem limite (ex.: salário). */
    MONTHLY,
    /** Recorrência por quantidade fixa de meses (ex.: parcela em 5x). */
    LIMITED_MONTHS
}
